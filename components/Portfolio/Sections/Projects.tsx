import SectionContainer from "./index";

export default function Projects() {
    const titles = ["02- Projects"]

    return <SectionContainer titles={titles} height="60vh">
    </SectionContainer>
}